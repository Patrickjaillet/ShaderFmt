{
  "name": "shaderfmt-release-tooling",
  "private": true,
  "version": "0.1.0",
  "description": "Release automation (semantic-release) for ShaderFmt. Not a published package - the actual project is the C++ code in lib/ and app/.",
  "devDependencies": {
    "semantic-release": "^24.0.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1"
  }
}
